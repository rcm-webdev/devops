{
  "name": "prod-ready-devops-root",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.0"
  },
  "lint-staged": {
    "backend/**/*.js": [
      "npm run lint --prefix backend --"
    ],
    "frontend/src/**/*.{js,jsx}": [
      "npm run lint --prefix frontend --"
    ]
  }
}
